# Archive tests (parking lot)

Lege parkplek voor tests die bij gearchiveerde modules horen.

Vitest excludeert `tests/archive/**` (zie `vite.config.ts`). Voeg tests hier alleen toe samen met bijbehorende code onder `src/archive/`.
