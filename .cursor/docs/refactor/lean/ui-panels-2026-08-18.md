# Lean delta — Cluster 4 UI panels — 2026-08-18

## Meta

| Veld | Waarde |
|------|--------|
| Cluster | 4 — Settings / Preprocess / ProjectSetup / WorkspaceView |
| Status | **Skip VERBETER** — geen G+I |
| Bron | knip 2026-08-18; lean ui-ux 2026-07-28; plan lean_dry_campagne |
| Gate | Alleen batches als DOCUMENT **G én I** toont |

## Verdict

**Meestal gezond.** Grootte alleen (G) telt niet; er is **geen actionable I** binnen één package. Cluster 4 **overslaan** of hoogstens een cosmetische mini-batch (niet aanbevolen).

## INDEX

| Pad | ~Regels | Rol | G+I? |
|-----|--------:|-----|------|
| `UserSettingsView.vue` | 642 | settings form + i18n/import | G zonder I |
| `PreprocessPanel.vue` | 648 | B/W-tune UI → `layer-preprocess` | G zonder I |
| `ProjectSetupPanel.vue` | 599 | stap-0 meta/floors/defaults | G zonder I |
| `WorkspaceView.vue` | 773 | host wiring + Dev panels | host, geen I |

## Findings (alleen relevant)

### G — context (geen actie)

Alle vier >500 regels: form/host-dichtheid, geen orchestrator-logica. Geen god-split “om regels”.

### I — Inline duplicaat

| Kandidaat | Waarom **geen** batch |
|-----------|------------------------|
| Heights/dikte/bovenlicht in Settings ≈ ProjectSetup | Twee surfaces: `draft.defaults` (user) vs `emit('update:floorDefaults')` (floor). Layout verschilt (grid vs categories + `showBovenlichtMeasures`). Extract = cross-surface coupling, geen package-I. |
| Preprocess slider×N (range+number) | Al `readField`/`writeField`/`PreprocessActionGroup`. Template-helper = O-risico, geen DRY-winst. |

### F — product gates (NIET verwijderen)

| Gate | Waar |
|------|------|
| `FML_ROOM_TAG_COLOR_SETTINGS_VISIBLE` (=false) | `UserSettingsView` `v-if` roomtag-sectie |
| `GAPS_TAB_VISIBLE` | `PreprocessPanel` copy-targets |
| Dev/tab flags via `useWorkspaceViewUi` | `WorkspaceView` Dev panels — techniek blijft |

## Voorgestelde batches

1. **Skip** — geen VERBETER.
2. *(Optioneel, cosmetisch)* Preprocess range+number → één lokale macro — **niet** aanbevolen; diff ≫ winst.

## Niet doen

- God-split panels / `WorkspaceView` opnieuw hosten
- Shared `FmlDefaultsForm.vue` Settings↔Project zonder productbesluit
- `v-if=false` / `*_VISIBLE=false` “dode” takken knippen
