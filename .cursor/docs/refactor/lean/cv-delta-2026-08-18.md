# Lean delta — Cluster 5 CV (sinds 2026-07-29) — 2026-08-18

## Meta

| Veld | Waarde |
|------|--------|
| Cluster | 5 — CV delta-only |
| Knip | **187** unused exports (hele `src`); **~72** raken `cv/**` |
| Status | inventaris; **geen** L5–L10 her-split |
| Lean baseline | walls/doors/windows lean 2026-07-28 **closed** |

## Verdict

Geen nieuwe I/R die een CV-structuurbatch rechtvaardigt. Enige lean-batch: **B unexport** op orphan exports (vooral `window-wall-dedupe` + stamp/ref-door orphans). Oblique + diagnose = gezond / F.

## NEW modules (git A sinds 2026-07-29)

| Module | ~Regels | I/R? | Actie |
|--------|--------:|------|-------|
| `engines/oblique/*` + `policies/oblique.ts` | ~770 | Geen | **F** — leave; entry via L10 |
| `windows/window-wall-dedupe.ts` | 148 | Orphan exports | **B** unexport helpers |
| `preprocess/wall-stamp-raster.ts` | 325 | `STAMP_GRAY_INK` orphan | **B** |
| `refs/ref-door-orient.ts` | 278 | 2 orphan fns | **B** |
| `room-wall-face-mask.ts` | 56 | — | thin helper; **geen** face-class god |
| `platform/export/diagnosis-report.ts` | 653 | groei HTML | **F** — scale al in `diagnosis-scale-overlay` |

`offAxisDeg` in oblique vs `core/fml/orthogonalize-near-axis-walls` = naamgelijk, andere domain → **F** (niet mergen).

## F / NO-GO

| Item | Reden |
|------|-------|
| L5–L10 her-split / Cat C | lean closed; product-F |
| `baseline.ts` + `BaselineResult` | niet aanraken |
| L6 connector types (`ChamferGroup*`, `Layer6JunctionKindCounts`, `Baseline*Junction*`) | historisch F |
| Star-barrels `doors/index` / `windows/index` | tenzij tree-shake pijn |
| `segment-ops` / `prune` rename | geen C-finding |
| Dual/claim/adjacency parallel | owners freeze |
| Diagnose HTML “opsplitsen om regels” | payload-builder, geen I |
| Oblique package split | al lean; alleen L10 wire |

## Safe B — unexport (knip orphan, intern gebruikt)

**Prioriteit 1 (nieuw + duidelijk):**

- `window-wall-dedupe.ts`: `WINDOW_DEDUPE_*`, `DOOR_WINS_*`, `wallAxisInterval`, `intervalIntersection`, `intervalIou`, `intervalOverlapMinRatio` — behoud export `dedupeOverlappingBoundWindows` + `suppressWindowsNearDoors` (+ types indien barrel nodig)
- `wall-stamp-raster.ts`: `STAMP_GRAY_INK`
- `ref-door-orient.ts`: `resolveDoorWallAxis`, `pickHeaviestFace`

**Prioriteit 2 (knip orphan, package-intern; unexport leaf of barrel-reexport):**

- doors: `doorTouchesKeptWallMask`, `buildDoorSwingMask` + `CLUSTER_SWING_CLOSE_KERNEL_PX` (re-export), `hypothesisTouchesWall`, `maxCentroidSegmentDistancePx`, `unionBBoxBounds`
- windows: `MAX_PAIR_AXIS_SPAN_RATIO`, `collectTouchedDoorArcFaceIds`, `WINDOW_EVIDENCE_TUNING` re-export in filter, `unionBBox` in evidence-geom
- collapse barrel: `segmentsThicknessCompatible`, `topologyPreservedAfterCollapse` (+ stats-types noise)
- walls misc: `syncPinnedClassOverrides`, `countMergedSurfaces`, `renderMergedWallCloseOverlay`, `THICKNESS_CORE_FRACTION`, `buildDistanceMapFromMask`, `renderRoomWallSkeletonOverlay`, `COLLAPSE_*_SHARED`
- refs: `classifyWallRefStyleFromImage`, `toRefFaceProfileWithDual`, `orientDoorBwSwingToBottom`, `rankSwingSectorFaces` (keep `*ForPick` path)

## Leave alone (knip-noise / F)

| Export | Waarom |
|--------|--------|
| L6/connector exported **types** | F baseline |
| `DoorSpacePolicy`, `PipelineV3Debug`, `CvPipelineConfig`, `PipelineDebugResult` | contract / archive |
| `ObliqueRebuildStats`, `RidgeProbe` (barrel) | L10/tests/journal |
| `hasArcLikeContour` dual path (`ref-swing-hinge*`) | shared intentional |
| `GapLayerId`, preprocess tab types, polygon/raster skeleton types | type-surface F |
| Public entry functions of NEW modules | niet knippen |

## Batches (na go)

1. **B1** — window-wall-dedupe unexport constants/helpers (~15 regels API) — tests: `window-wall-dedupe.spec`
2. **B2** — stamp + ref-door-orient orphans
3. **B3** — optioneel rest P2 unexports (één package per PR)

**Niet:** oblique herschrijven; diagnose god-split; L6 aanraken.
