# Lean rapport — Stap 4 FML — 2026-07-28

## Meta

| Veld | Waarde |
|------|--------|
| Cluster | Stap 4 FML (extractionToPlan → preview/editor → buildFmlV3) |
| Scope-paden | `core/fml/**`, `useWorkspaceFml*`, fml-preview render/selection |
| Status | **B1–B4 done**; `hasFmlSemanticSource` gate = open F |
| Bron-refactor | [`../stap4-fml-2026-07-25.md`](../stap4-fml-2026-07-25.md) |
| Gerelateerde docs | door-mirrored-semantics, export-options |

## Samenvatting

- Conversie lean na ronde 9; editor deur↔raam = hoofdseam **N/I** (F).
- B1–B4 geïmplementeerd: defaults/clamps, draft helper, 1× graph/origin, shared edge-filter.

## INDEX

| Pad | ~Regels | Rol | Owners |
|-----|---------|-----|--------|
| `core/fml/extractionToPlan.ts` | ~170 | entry + WithOrigin | semantic + L12/L14 |
| `extraction-to-plan-{types,geom,walls,doors,windows,edge-openings}.ts` | helpers | geom; doors merge; edge filter |
| `core/fml/layer-openings-to-fml.ts` | ~45 | opening DTO | OrientedDoor / BoundWindow |
| `core/fml/buildFmlV3.ts` | ~160 | FML shell | Floorplanner |
| `ui/.../workspace-fml-generate.ts` | ~250 | generate bundle | preview priority |
| `fml-preview-opening-draft.ts` | ~70 | mixed draft | selection + panel |
| `ui/components/fml-preview-openings.ts` | ~250 | IDs/clamps | `DEFAULT_FML_*` |

## Call-flow (kort)

- L12/L14 → `toLayer*` → `extractionToPlanWithOrigin` (1× graph) → harmonize → `previewPlan` → `buildFmlV3` / editor.
- Generate-pass: `generatedBundle` deelt plan + origin met underlay.
- Editor: deuren als groepen; ramen los; `computeOpeningDraftState` voor sync+panel.

## Findings (na VERBETER)

### I / R / N — gedaan in B1–B4

| Item | Batch | Resultaat |
|------|-------|-----------|
| `DEFAULT_FML_*` + resolve height/sill | B1 | imports; Panel props blijven lokaal F |
| `clamp01` / span gate 0.5 / renames | B1 | geom owner; `clampOpeningWidth`; `clearOpeningSelectionState` |
| Opening mixed-draft | B2 | `computeOpeningDraftState` |
| cm-origin + resolveGraph 3× | B3 | `FromGraph` + `WithOrigin` + underlay via bundle |
| mapLayer12/14 filter | B4 | `filterOpeningsForEdge` + `openingSpanOnEdge`; merge/mirrored deur-only |

### F / P — open / bewust

| Item | Reden |
|------|-------|
| `hasFmlSemanticSource` ↔ `generatedPlan` segments-only | product-go; breaking voor segments-only pad |
| Deur group-render vs raam per-opening | domain |
| `mirrored` alleen deuren | floorplanner-semantiek |
| Panel/Toolbar prop defaults lokaal | magic-sync risico |

## Verificatie

- [x] vitest extractionToPlan / build-fml-v3 / fml-preview / use-workspace-fml (53)
- [ ] UI-smoke Project4: select/move/spiegel/copy + underlay + download + twin double_wide
- build: pre-existing TS-schuld elders (geen FML-regressie in foutlijst)

## Log

| Datum | Fase / Batch | Resultaat |
|-------|--------------|-----------|
| 2026-07-28 | INDEX/DOCUMENT/DISCUSS | inventaris |
| 2026-07-28 | B1 | DEFAULT_FML_*; clamp01; span 0.5; renames — 53 groen |
| 2026-07-28 | B2 | `computeOpeningDraftState` → sync + panel |
| 2026-07-28 | B3 | `resolveExtractionCmOriginFromGraph` + `extractionToPlanWithOrigin`; underlay uit bundle; **geen** hasFmlSemanticSource-gate |
| 2026-07-28 | B4 | `filterOpeningsForEdge` + `openingSpanOnEdge`; merge/mirrored deur-only — 53 groen |
