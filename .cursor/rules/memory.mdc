---
description: Sessie-geheugen – huidige staat en lopende werkzaamheden
alwaysApply: true
---

# Memory – BouwToFML

Laatste update: 2026-08-22 (aanzicht dakplaat om het hart)

## Huidige focus

**Dak-tab + dakvlakken** — rail-chip Dak (naast Gevels) opent Dak-design overlay. Generate via sidebar (FmlEditor expose). Nok wint van volgende vloer. Dakramen/overstek later.

## Wat WERKT

**Flow:** Onderlegger → Voorbewerking → Detectie → Resultaat. Contract: `.cursor/docs/workspace-flow.md` + `layer-flow.ts`.

**Canvas chrome:** `FmlEditorTopbar` op stap 1–4 (raster + FML). Hints via **i**-modal (`workspace-canvas-help.ts`); geen hint-balk. Fullscreen op alle canvas-stappen. Mask/inkt-history met redo (Ctrl+Shift+Z/Y). Sidebar: `sidebar-chrome.css` icon+naam, zebra weg.

**Stap 1:** schaal/rotatie/crop/gum. Optionele «Rotatie vastzetten» (zelfde bake als 1→2). Geen auto-trim van witte randen; canvas wit. Geen refs. Floor **min 3000px**. Asymmetrie-waarschuwing H/V >2%.

**Stap 2:** B/W-tune + **LBE-refs** (max 3 muur min/mid/max) + OCR-toggle (default uit). Afronden meet dikte/stijl op post-bake **`baseBw`**. `effectiveBw = baseBw ⊕ ocrMask ⊕ inkOverlay`. Muurstempel (opt. Stempelset: translate-only + nulpunt-zaad) + B/W-overnemen. Stempel buiten scan → wit pad (`expandUnderlayForStamp`). Fabrieksreset = alleen laag-tune.

**Stap 3:** altijd Muren; OCR/Deuren/Ramen → Dev-view. Muren = room-first → V3 L1–L10. Deuren Stage-2 → L11/L12. Ramen Stage 1–4 → L14. Face-class: V3 ziet alleen binary mask. Stempel-inkt blijft zichtbaar in classify.

**Stap 4 / persist:** `previewPlan` + IndexedDB (`projects` + `projectIndex`). Resume «Verder werken». Diagnose-FAB = HTML met stap-1 scan + linialen + L1–L14. Stempelset-inject na nulpunt (`injectStampWallsIntoPlan` add-only) + ownership ronde 2 (`resolve-stamp-ownership`, stamp frozen cm/dikte, det→stamp); dikte pinned. Generate ná semantic + deuren/ramen; sanitize weld/ortho/T-X/cover tot vast punt (max 4).

**FML workspace:** generate + editor (select/move/spiegel/copy/draw). Canvas-top = zelfde topbar als editor/inspect (geen hint-balk; undo/fit/fullscreen/zoom). Sidebar sluiten + menu-FAB (fullscreen sluit sidebar). Areas/surfaces-edit **uit** (`FML_AREA_SURFACE_EDIT_VISIBLE`). Oriëntatie-UI **uit** (`FML_ORIENT_CONTROLS_VISIBLE`); techniek blijft. Herschalen = H/V-linialen (geen muurdikte).

**FML `/FML-editor`:** leeg plan + floor CRUD + onderlegger-stretch (alleen `drawing`). Inspect-modus. Mobile chrome + touch alleen `pointer: coarse`. Extra gate `J0rd!`. Sessie-defaults + `az`/`bz`. Sanitize-knop (weld+ortho+T/X+cover tot stabiel); import/download = junction-pass. Gevelgroepen. Chrome-dialogs. Fixture-dock linksonder. Sticky select; click-move-click tekenen. Balance keep-axis. Roundtrip bewaart envelope als `source` er is. **Stempel:** checkbox naast gevel; Plattegrond «Stempel» kopieert bron-muren naar actieve floor; download stript `stamp`. **Afmetingen:** Autogen/Slicer/Handmatig (vis exclusief); slicer `btfSlices` + M/P-handles; meet-tool Tape/Manual/Slicer. **Muur-slide:** T-tak voorbij stam-einde absorbeert overlap (geen dubbele muur). **Surface-polygoon:** rubber-band + H/V-snap; hoeken/ribben van andere surfaces/areas (Shift = vast, Ctrl = uit). **Gevels:** chip ná `+` (alleen met groepen); projectie + hoogte-stack + per-groep onderlegger; schaallinialen + verplaatsen (zelfde als plattegrond). **Nok:** Muur/Nok-dropdown; eigen junction-graaf (geen weld met plattegrond); junction+segment-sleep; nokhoogte bij tool+Nok overschrijft bestaande; Dak-design overlay; dashed `displayWidthCm`; kopgevel-balk gecentreerd. **Dakvlakken:** rail-tab Dak (stippellijn-muren, alleen Nok+polygoon); nok op hoogste floor die het stuk dekt + dropdown verdieping. Ctrl+klik polygoon + punthoogte. Aanzicht = goot-rakend vlak + plaatdikte = nokdikte; Ctrl+klik + punten alleen hoogte.

**Host:** `floorplan-fml` workers.dev; SPA via `not_found_handling` (geen `_redirects`). Soft access-gate. i18n `en`/`nl`/`th`.

## Open / handmatig checken

- Dakvlakken: tab Dak = alleen Nok + polygoon; klik vlak = knopen + hoogte; Ctrl+klik = punten; snap = buitenfaces, hoek = snijpunt van twee goten (niet face+hartlijn). Overstek/dakkapel later.
- Nok-muren: Muurtool → Nok; tekenen snapt op binnen-/buitenface (niet knoop); nokhoogte meteen (lengte pas na 1e punt); junction+segment-sleep in eigen graaf; convert muur↔nok; aanzicht toont piek op kopgevel; download houdt Dak-design; leeg Dak (zonder vlakken) valt weg.
- Gevels-aanzicht: volle baksteen buiten–buiten + binnenkant stippellijn; bovenkant-hoogte vanaf hartlijn (oren recht omhoog). Split-tool + knooplijn-hoogte; vloerbanden + nokbalk; projectie Architect/Projectief; onderlegger schaal+move; inspect/detectie nog niet. Dakvlak = goot of kopse kil (grijs) + plaatdikte om het hart; goot = getekende buitenhoek. Ctrl+klik dakvlak = punten alleen hoogte.
- Editor maatlijnen: Autogen/Slicer/Handmatig; slicer P→M + H/V; linialen zichtbaar bij slicer-tool; handmatig oneindige face-as-snap; meettype-dropdown direct bij tool.
- Muur-slide voorbij T: linker hal/woonkamer-muur niet verdubbelen; bestaande dubbele → Opschonen of opnieuw slepen.
- Stempel ownership A+r2: freeze stamp cm/dikte; det→stamp junction; full-drop ≥50%; bake inject-count. Band raster-only.
- UI-polish: stap 0 één +; close-bulge; stap 2 geen B/W-overnemen; classify-icoon; rescale left+icon.
- UI-cleanup: stap 0 icon-knoppen; stap 1 crop/gum undo-redo + FS; stap 2 B/W/stempel + zoom; stap 3 classify + i-tips; stap 4 topbar ongewijzigd.
- Stempelset: donor met stamp-leden → stap 2 bake → detect → stap 4: diktes = donor; nulpunt-sleep + regenerate houdt scan-pixels; Opschonen na afronden = no-op (sanitize al stabiel).
- Canvas 1–4 wit: rotatie-bake houdt witte hoeken; gum vult wit zonder canvas te krimpen; stamp overflow groeit nog als de stempel buiten de scan valt.
- Stempel buiten de tekening: onderlegger groeit met wit; bake/download neemt die lijnen mee; linialen/refs blijven op de scan.
- Editor-stempel: markeer muren → andere floor → Plattegrond Stempel; gevel+stamp dual; download zonder stamp.
- Stap-4 FML-topbar: geen tooltip, zelfde chrome als editor/inspect; fullscreen verbergt sidebar+header.
- Recente FML-smoke (dialogs, T-split+gevel, underlay-pivot, fixture-dock, sticky/touch).
- `bg` muur-shift 17,4 cm uit E2E-baseline-verversing (los van schuine-gevels).
- Kinderdijkstraat / smoke end-to-end nog niet handmatig.
- Ramen: per-ref Template ID UI; L14+merge+FML wel actief.
- Deur dual-space: shallow underdeeps.
- `hasFmlSemanticSource` gate = F.
- `vitest run` hangt soms na laatste test op Windows (inhoud OK).

## Wat NIET / half

- Ink-resolve/gaps: window als wall-boost nog niet aangescherpt.
- Archive-openings ≠ actieve deurpad.
- Wall-orientation-audit findings (doc).
- Diverse detectie-POC’s (deur-hoek, raam-clustering, gearceerde muren) — zie `decisions.md` «Nog open».
- Band-stempel (zonder set) blijft raster-only — geen vector-inject.

## Technische schuld

- Layer-preprocess tuning handmatig; opening-defaults conservatief.
- Raw-ink overlay = alle zwarte inkt (incl. tekst/ruis).
- L6 connector ~35 files; Cat C generalisatie = product-F.
- Pipeline-V3 Cat C + meer fixtures buiten BouwTek11/2D_3E = apart product-go.
- Lean/DRY + production-refactor ronde 1–22 **klaar**; clusters leeg.
- `FloorplanToolbar.vue` ongebruikt (vervangen door `FmlEditorTopbar`).

## Architectuur (niet wijzigen zonder overleg)

- Geometry-first + per-laag preprocess; rotatie/crop op Origineel; geen maxres-downscale in CV.
- Enige muurpad: room-first + V3 L10. OCR alleen stap-3 UI (`useWorkspaceOcr`), niet in `geometry-pipeline`.
- `tabOutputs` alleen vullen in detectie-run; `isValidTabOutput`.

## Niet aanraken

- `frontend/src/cv/port/baseline.ts` Hough-baseline parameters.

## Recente besluiten

- **2026-08-22** — Aanzicht-dakvlak: plaatdikte om het hart (helft boven/onder); kopgevel toont beide vlakken via kil (geen Z-eis; vul ná dikte).
- **2026-08-22** — Aanzicht-dakvlak: goot volgt de getekende buitenhoek, niet de hartlijn (anders alleen de gevel mét zolder-muur smaller).
- **2026-08-22** — Aanzicht-muren weer volle baksteen + binnen-stippellijn; alleen bovenkant-hoogte vanaf hartlijn (geen kuntjes).
- **2026-08-21** — Aanzicht-dakvlak: plaatdikte = `nokThicknessCm` **omhoog** (FML heeft geen surface-thickness). Handles blijven op het vlak.
- **2026-08-21** — Nok-tekenen: snap op binnen-/buitenface zoals maatlijnen, niet junction/hartlijn. Ctrl = vrij.
- **2026-08-21** — Aanzicht-dakvlak: Ctrl+klik = punten; sleep alleen hoogte (X/Y vast). Snap 8 cm op andere punten/muurtop.
- **2026-08-21** — Aanzicht-dakvlak: alleen goot-rakend vlak (grijs per gevel). Geen zijgevel-projectie; andere gevels niet bedienen.
- **2026-08-21** — Dakvlak-snap: alleen buitenfaces; hoek = snijpunt van twee goten (niet één face + hartlijn). Faces stoppen op hartlijn-einden — daarom expliciet hoek snijden.
- **2026-08-21** — Aanzicht-projectie: Architect (vaste H/V, default) vs Projectief (mee met gevel); sidebar Gevels. Schuine AMS-gevel: architect = kopse nok.
- **2026-08-21** — Nok-flow: hoogte is world-Z (niet hetzelfde relatieve getal per floor). Nieuwe nok gebruikt doelverdieping-hoogte als de draft onder die vloer zakt. Aanzicht = lengte + dwarsdikte, zelfde kop links/rechts. Sidebar Nok = dakspan.
- **2026-08-21** — Gevel-onderlegger: schaallinialen + verplaatsen op `FmlElevationHost` (was alleen sidebar; plattegrond-canvas kreeg de props). Scale-px via elevation-layout; persist bij groeps-wissel/download.
- **2026-08-21** — Dak-tab: muur-afdruk = baksteen binnen+buiten stippellijn (geen hartlijn); generate alleen op floors mét nok.
- **2026-08-21** — Dak-tab: alleen Nok + polygoon; nieuwe nok op hoogste dekkende floor; dropdown om te verplaatsen.
- **2026-08-21** — Dak-tab op floor-rail (naast Gevels); generate via FmlEditor expose; nok wint van volgende vloer (zelfde omtrek gaf 0 vlakken).
- **2026-08-21** — Nok-edit: eigen junction-graaf (geen weld met plattegrond); junction+segment-sleep zoals muren; nokhoogte bij muurtool+Nok (niet pas na 1e punt); selectie past `az`/`bz`.z.
- **2026-08-21** — Dakvlakken op Dak-design (`surfaces[]`, `isRoof`); knop-snapshot nearest+split; heup in v1; `manual` overleeft regen; geen kopgevel-flag (nok raakt gevel).
- **2026-08-21** — Nok-muren in sibling Dak-design (`btfRole: ridge`); geen weld/T met plattegrond; overlay-tekenen; `settings.ridgeWalls`; aanzicht = gevelgroep + nokken.
- **2026-08-21** — Gevels-aanzicht editor-only: 1 groep = 1 projectie; hoogtes/vloer/nok gedeeld (`floorStack`); openings per GUID; onderlegger in `elevationViews`. Chip ná `+` (niet rechts uitlijnen). Geen inspect/detectie.

Detailbesluiten → `.cursor/docs/decisions.md`. Catalogus-refids (Poort6, Anna, Ostade, Kinderdijk) staan daar / in eerdere sessies, niet hier.

---

## Onderhoud (voor de agent)

**Wanneer:** einde substantiële sessie; na expliciet besluit; bij WERKT ↔ NIET ↔ schuld.

**Hier vs elders**

| Onderwerp | Plaats |
|-----------|--------|
| Sessiestaat, lopend werk | **dit bestand** |
| Principes, stack, scope | `project-context.mdc` |
| Formele beslissingen | `.cursor/docs/decisions.md` |
| Testartifacts | `.cursor/tests/artifacts/` |

**Hoe:** datum bijwerken; bullets kort; verouderd weg; besluiten `YYYY-MM-DD — wat — waarom`; geen proza/changelog. Max ~richtlijn: focus + werkt + open + ≤15 recente besluiten.

**Niet:** invullen zonder observatie; dupliceren van project-context/decisions; commit-notities.
